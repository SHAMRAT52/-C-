#include<iostream>
using namespace std;

int main()
{

    int a=5,b=6,c=5;
    cout<<"The result is for comparison between a and b(a>b) is= "<<(a>b);
    cout<<"\n\nThe result is for comparison between a and b (a<b) is= "<<(a<b);
    cout<<"\nThe result is for comparison (a==b) is= "<<(a==b);
    cout<<"\nThe result is for comparison (a!=b) is= "<<(a!=b);

    cout<<"\n The result is for comparison (a>=c) is= "<<(a>=c);
    cout<<"\n The result is for comparison (a<=c) is ="<<(a<=c);




    return 0;
}
